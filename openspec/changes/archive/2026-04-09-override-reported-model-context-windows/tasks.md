# Tasks

- [x] T1: Add OpenSpec delta requirements for configurable reported model context window overrides
- [x] T2: Add validated `model_context_window_overrides` settings support
- [x] T3: Apply context window overrides consistently across `/api/models`, `/v1/models`, and `/backend-api/codex/models`
- [x] T4: Add integration tests for overridden and default model metadata behavior
- [x] T5: Run targeted tests and validate OpenSpec specs
