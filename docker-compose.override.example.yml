services:
  server:
    volumes:
      - ${CODEX_LB_DATA_DIR:-./.local/codex-lb-data}:/var/lib/codex-lb

  frontend:
    # Example placeholder to show where per-developer overrides belong.
    # Add extra ports, env vars, or bind mounts here if your local workflow needs them.
    environment:
      CHOKIDAR_USEPOLLING: "${CHOKIDAR_USEPOLLING:-false}"
